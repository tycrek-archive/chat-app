{
	"port": 34682,

	"queries": {
		"segments": ["account", "authentication"],
		"account": {
			"create": "account.create.sql",
			"info": "account.info.sql"
		},
		"authentication": {
			"credential": "authentication.credential.sql",
			"token": "authentication.token.sql",
			"expire": "authentication.expire.sql"
		}
	}
}